# Port on which the backend server runs
PORT = 5000

# Google Cloud Project ID
PROJECT_ID = your-google-cloud-project-id

# Secret Name to fetch MongoDB URI (Update to your user-specific secret name)
SECRET_NAME = your_secret_name_here

# MongoDB connection string for MongoDB Atlas
# Replace <username>, <password>, and <dbname> with your specific values.
MONGO_URI = mongodb+srv://<username>:<password>@cluster1.ldnqk.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=Cluster1

# JWT Secret for authentication
JWT_SECRET = your_jwt_secret_here